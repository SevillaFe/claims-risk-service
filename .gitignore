__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
report.xml
# El modelo se genera en el build, no se versiona
app/model.joblib
# Terraform: el state NUNCA va a git (puede contener secretos)
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
terraform.tfvars
